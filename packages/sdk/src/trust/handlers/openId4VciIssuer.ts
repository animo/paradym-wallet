// TODO(sdk): is this the best location for this type?
export type TrustedOpenId4VciIssuerEntity = {
  issuer: string
  name: string
  logoUri: string
  url: string
  demo?: boolean
  entityId: string
}
