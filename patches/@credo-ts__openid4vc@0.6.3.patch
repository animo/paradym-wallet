diff --git a/build/openid4vc-holder/OpenId4VciHolderService.mjs b/build/openid4vc-holder/OpenId4VciHolderService.mjs
index 77e89e2cad1a24e0863a30f2fab308a94a02187e..9cd99e40aaf086e6549e67b3e2543f0f1e63ee96 100644
--- a/build/openid4vc-holder/OpenId4VciHolderService.mjs
+++ b/build/openid4vc-holder/OpenId4VciHolderService.mjs
@@ -593,6 +593,7 @@ let OpenId4VciHolderService = class OpenId4VciHolderService {
 		const credentials = credentialResponse.credentials ? credentialResponse.credentials.every((c) => typeof c === "object" && c !== null && "credential" in c) ? credentialResponse.credentials.map((c) => c.credential) : credentialResponse.credentials : credentialResponse.credential ? [credentialResponse.credential] : void 0;
 		if (!credentials) throw new CredoError(`Credential response returned neither 'credentials' nor 'credential' parameter.`);
 		const notificationId = credentialResponse.notification_id;
+		const credentialMetadata = credentialResponse.credential_metadata;
 		const format = options.format;
 		if (format === OpenId4VciCredentialFormatProfile.SdJwtVc || format === OpenId4VciCredentialFormatProfile.SdJwtDc) {
 			if (!credentials.every((c) => typeof c === "string")) throw new CredoError(`Received credential(s) of format ${format}, but not all credential(s) are a string. ${JSON.stringify(credentials)}`);
@@ -626,7 +627,8 @@ let OpenId4VciHolderService = class OpenId4VciHolderService {
 					}),
 					notificationId,
 					credentialConfigurationId,
-					credentialConfiguration
+					credentialConfiguration,
+					credentialMetadata
 				};
 			}
 			const result = await Promise.all(credentials.map(async (c) => {
@@ -644,7 +646,8 @@ let OpenId4VciHolderService = class OpenId4VciHolderService {
 				record: new W3cV2CredentialRecord({ credentialInstances: result.map((r) => ({ credential: r.credential.encoded })) }),
 				notificationId,
 				credentialConfigurationId,
-				credentialConfiguration
+				credentialConfiguration,
+				credentialMetadata
 			};
 		}
 		if (options.format === OpenId4VciCredentialFormatProfile.JwtVcJson || options.format === OpenId4VciCredentialFormatProfile.JwtVcJsonLd) {
@@ -670,7 +673,8 @@ let OpenId4VciHolderService = class OpenId4VciHolderService {
 				}),
 				notificationId,
 				credentialConfigurationId,
-				credentialConfiguration
+				credentialConfiguration,
+				credentialMetadata
 			};
 		}
 		if (format === OpenId4VciCredentialFormatProfile.LdpVc) {
@@ -697,7 +701,8 @@ let OpenId4VciHolderService = class OpenId4VciHolderService {
 				}),
 				notificationId,
 				credentialConfigurationId,
-				credentialConfiguration
+				credentialConfiguration,
+				credentialMetadata
 			};
 		}
 		if (format === OpenId4VciCredentialFormatProfile.MsoMdoc) {
@@ -726,7 +731,8 @@ let OpenId4VciHolderService = class OpenId4VciHolderService {
 				})) }),
 				notificationId,
 				credentialConfigurationId,
-				credentialConfiguration
+				credentialConfiguration,
+				credentialMetadata
 			};
 		}
 		throw new CredoError(`Unsupported credential format ${options.format}`);
diff --git a/build/openid4vc-holder/OpenId4VciHolderServiceOptions.d.mts b/build/openid4vc-holder/OpenId4VciHolderServiceOptions.d.mts
index eda65a22d888509e4349f499ce551b91a1442f50..97996fc4add8ab7b68a3b75d8f68d31540357b0e 100644
--- a/build/openid4vc-holder/OpenId4VciHolderServiceOptions.d.mts
+++ b/build/openid4vc-holder/OpenId4VciHolderServiceOptions.d.mts
@@ -41,6 +41,15 @@ interface OpenId4VciCredentialResponse {
    */
   record: SdJwtVcRecord | MdocRecord | W3cCredentialRecord | W3cV2CredentialRecord;
   notificationId?: string;
+  /**
+   * Credential metadata returned in the `credential_metadata` parameter of the
+   * Credential Response as defined in OpenID4VCI 1.1 (Section 9.3).
+   *
+   * Per spec, this metadata follows the same structure as `credential_metadata`
+   * in the Credential Issuer metadata (Section 13.2.4) and, when present,
+   * overrides any metadata sourced from the Issuer or Credential Format profile.
+   */
+  credentialMetadata?: Record<string, unknown>;
 }
 interface OpenId4VciDeferredCredentialResponse {
   credentialConfigurationId: string;
