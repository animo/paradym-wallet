name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

# Cancel multiple runs for the same PR or branch
concurrency:
  group: aries-framework-${{ github.ref }}-${{ github.repository }}-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-22.04
    name: Validate
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: yarn

      - name: Install dependencies
        run: yarn install

      - name: Linting
        run: yarn lint

      - name: Prettier
        run: yarn check-format

      - name: Check Types
        run: yarn check-types

      - name: Compile
        run: yarn build
### This is to automate the integration tests. I think it will be slow and there are some things left to do to make it work
# TODO: Add keychain to workflow so that we can build and run the app
# TODO: Create a way to build -> install -> run the app on the simulator
#
#  maestro:
#    runs-on: macos-11
#    name: Maestro Test
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#
#      - name: Setup Node
#        uses: actions/setup-node@v3
#        with:
#          node-version: 18.x
#          cache: yarn
#
#      - name: Install dependencies
#        run: yarn install
#
#      - name: Install Maestro
#        run: url -Ls "https://get.maestro.mobile.dev" | bash
#
#      - name: Add Maestro to current shell
#        run: export PATH="$PATH":"$HOME/.maestro/bin"
#
#      - uses: futureware-tech/simulator-action@v2
#        with:
#          model: 'iPhone 13 Pro'
#
#      - name: Compile and run
#        run: yarn ios --no-install
#
#      - name: Run Maestro
#        run: maestro test --output ./.maestro/test-results.xml --format junit ./.maestro/App-Flow.yaml
#
#      - name: Publish Test Report
#        uses: mikepenz/action-junit-report@v3
#        if: success() || failure() # always run even if the previous step fails
#        with:
#          report_paths: './.maestro/test-results.xml'
